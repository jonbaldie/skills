interface:
  display_name: "Carnival"
  short_description: "Open a carnival — this session becomes mayor"
  default_prompt: "Use $carnival to open a carnival in this session."
policy:
  allow_implicit_invocation: false
