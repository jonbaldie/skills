interface:
  display_name: "Outpost"
  short_description: "Open an outpost — this session becomes prefect"
  default_prompt: "Use $outpost to open an outpost in this session."
policy:
  allow_implicit_invocation: false
